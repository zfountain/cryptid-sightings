<!DOCTYPE html>
<html>
  <head>
    <title>Cryptid Sightings Across the World</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Roboto+Condensed' rel='stylesheet' type='text/css'>
    <%= favicon_link_tag 'favicon.ico' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <div id="wrapper">
      <header>
        <span id="site-title"><%= link_to 'Cryptid Sightings', "/" %></span>
        <div id="nav">
          <% if current_user %>
            <%= link_to "My Profile", user_path(current_user.id) %> |
            <%= link_to "Log out", :logout, method: :post %>
          <% else %>
            <%= link_to "Register", new_user_path %> |
            <%= link_to "Log in", :login %>
          <% end %>
        </div>
        <div>
          <p id="notice"><%= flash[:notice] %></p>
          <p id="alert"><%= flash[:alert] %></p>
        </div>
      </header>
      <div class="grid">
        <div class="col-2-3">
          <%= link_to 'Share an Encounter', new_encounter_path, :class => 'buttons-top' %>
          <%= link_to 'Add a Cryptid', new_cryptid_path, :class => 'buttons-top' %>
          <%= yield %>
        </div>
        <div class="col-1-3">
          <div class="cryptids-list">
            <%= link_to 'Share an Encounter', new_encounter_path, :class => 'buttons-sidebar' %>
            <%= link_to 'Add a Cryptid', new_cryptid_path, :class => 'buttons-sidebar' %>
            <h2>Cryptids</h2>
            <ul id="cryptids">
            </ul>
          </div>
        </div>
      </div>
      <footer>
        <p>Don't be scared</p>
        <span>&copy; <%= Date.today.year %> <a href="http://zachfountain.com/" target="_blank" title="Zach Fountain's Website and Portfolio">Zach Fountain</a></span>

      </footer>
    </div>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </body>
</html>
